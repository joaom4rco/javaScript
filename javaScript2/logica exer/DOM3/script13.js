
function retangulo() {
    const base = Number(prompt('escreva a base da seu retangulo'))
    const altura = Number(prompt('escreva a altura da seu retangulo'))

    const calcArea = base * altura
    const calculo = Number(alert(`a área do seu rentangulo é ${calcArea}`))


}
retangulo()